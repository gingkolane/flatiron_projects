<%= form_with(model: sponsor, local: true) do |form| %>
  <% if sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sponsor.errors.count, "error") %> prohibited this sponsor from being saved:</h2>

      <ul>
      <% sponsor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :AGENCY %>
    <%= form.text_field :AGENCY %>
  </div>

  <div class="field">
    <%= form.label :SPONSOR_TYPE %>
    <%= form.text_field :SPONSOR_TYPE %>
  </div>

  <div class="field">
    <%= form.label :AGENCY_CLASS %>
    <%= form.text_field :AGENCY_CLASS %>
  </div>

  <div class="field">
    <%= form.label :NCT_ID %>
    <%= form.text_field :NCT_ID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
