<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @list.user.name %>
</p>

<h3><%=@list.name%></h3>
<ul>
  <% @list.investigators.each do |investigator| %>
  <li><%= link_to investigator.NAME_DEGREE, investigator %></li>
  <% end %>
  </ul>
  <%= link_to 'Edit', edit_list_path(@list) %> |
  <%= link_to 'Back', lists_path %>
    <%= button_to 'Delete List', @list , method: :delete, data: { confirm: 'Are you sure?' } %>
